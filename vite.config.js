import { defineConfig} from 'vite';

export default defineConfig({
    base: '/blackjack-refactor/',//Aqui le estamos dando la ruta del repositorio de github para que lo haga automatico y no tengamos que hacerlo en el index.html de forma manual.
})